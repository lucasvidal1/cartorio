server:
  port: 9564
spring:
  datasource:
    url: jdbc:h2:mem:cartorio
    username: cartorio
    password:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false